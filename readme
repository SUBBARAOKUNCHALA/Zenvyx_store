
//node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"   
// jwt to creation run this line
//must create admin mail password and place that password and email in .env folder


// this is admin secrate key
//node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

//web hook for payment gate way node -e "console.log(require('crypto').randomBytes(32).toString('hex'))" it will retuen secret key 
